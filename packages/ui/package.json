{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:local": "tsc -b && vite build",
    "build:dev": "tsc -b && vite build --stage dev",
    "build:prod": "tsc -b && vite build --stage production",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.27.2",
    "preact-iso": "^2.11.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
